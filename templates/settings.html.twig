{% extends "base.html.twig" %}

{% block title %}Settings{% endblock %}

{% block head %}
    {{ parent() }}
{% endblock %}

{% block content %}
    {% if islogged %}
    <span style="color:red">{{message}}</span>
    <span style="color:rgba(12, 92, 5, 0.63)">{{confirm}}</span>
    <div class="container">
            <form action="index.php?url=resetpassword" method="post">
                <label for="pssw">Password</label>
                <!--<input type="password" id="pssw" name="pssw" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" title="Must contain at least one number and one uppercase and lowercase letter, and at least 8 or more characters" required>-->
                <input type="password" id="pssw" name="pssw" required>
          
                <label for="pswc">Password Confirmation</label>
                <!--<input type="password" id="psswc" name="psswc" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" title="Must contain at least one number and one uppercase and lowercase letter, and at least 8 or more characters" required>-->
                <input type="password" id="psswc" name="psswc" required>
          
                <input type="submit" value="submit" name="submit">
            </form>
          </div>
    {% endif %}
{% endblock %}

{% block footer %}
    {{ parent() }}
{% endblock %}